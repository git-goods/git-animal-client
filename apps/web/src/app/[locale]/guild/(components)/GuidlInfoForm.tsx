/* eslint-disable @next/next/no-img-element */

export async function GuildInfoForm({
  guildId,
  icons,
  backgrounds,
}: {
  guildId: string;
  icons: string[];
  backgrounds: string[];
}) {
  return <></>;
  //   const [state, formAction] = useFormState(createGuildAction, initialState);

  //   return (
  //     <form action={formAction}>
  //       <GuildInfoFormClient icons={icons} backgrounds={backgrounds} />
  //       <p aria-live="polite">{state?.message}</p>
  //       <Button type="submit" mt="40px" mx="auto" disabled={pending}>
  //         Create Guild
  //       </Button>
  //     </form>
  //   );
}
