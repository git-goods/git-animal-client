// `sliderContainer`는 AnimalSliderContainerMobile 등 외부에서도 import하므로
// className 문자열 상수로 export 유지한다. (별도 panda style 파일 아님)
export const sliderContainer =
  'relative w-[1120px] h-[800px] mobile:w-[calc(100vw-40px)] mobile:h-auto';
